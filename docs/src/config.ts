export const repo = "https://github.com/trailbaseio/trailbase";

const config = {
  title: "TrailBase",
  description: "TrailBase - a fast, simple and open Firebase alternative.",
  site: "https://trailbase.io",
  repo,
};

export type Config = typeof config;

export default config;
