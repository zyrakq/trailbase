// Entry point — registers all custom elements in the trail-auth bundle.
// Import this file as an IIFE bundle via <script> tag in the host page.
export * from './trail-auth.ts';
export * from './trail-profile.ts';
