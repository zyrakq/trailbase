x-logging: &default-logging
  driver: json-file
  options:
    max-size: ${LOG_MAX_SIZE:-10m}
    max-file: ${LOG_MAX_FILE:-3}
    compress: ${LOG_COMPRESS:-true}

services:
  argiago-web:
    container_name: argiago-web
    image: argiago
    build: .
    restart: unless-stopped
    logging: *default-logging
    ports:
      - 4000:4000
    volumes:
      - argiago-traildepot:/workspace/app/traildepot

volumes:
  argiago-traildepot:
    name: argiago-traildepot