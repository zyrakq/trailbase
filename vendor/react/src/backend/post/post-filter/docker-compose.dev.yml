version: '3.8'

services:
  post-filter:
    container_name: post-filter
    image: "{{ .Values.werf.image.post_filter }}"
    labels:
      kompose.service.expose: "post-filter.argiago.test"
      kompose.service.expose.tls-secret: "post-filter-secret"
      cert-manager.io/cluster-issuer: cert-manager-issuer
      kompose.service.expose.ingress-class-name: "nginx"
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8080/healthcheck" ]
      interval: 20s
      retries: 5
      start_period: 20s
      timeout: 10s
    environment:
      ISSUER: https://keycloak.argiago.test/realms/argiago-test

      DB_URL: mongodb://root:root@${MONGO_SERVICE_HOST}:${MONGO_SERVICE_PORT}
      DB_NAME: post_store
      COLLECTION_NAME: post
    ports:
      - "80:8080"
    networks:
      - argiago-network
  
networks:
  argiago-network:
    name: argiago-network
    external: true
      